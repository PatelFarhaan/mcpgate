apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mcpgate.fullname" . }}
  labels:
    {{- include "mcpgate.labels" . | nindent 4 }}
data:
  KEY_PREFIX: {{ .Values.config.keyPrefix | quote }}
  CORS_ORIGINS: {{ .Values.config.corsOrigins | quote }}
